name: test_driver

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1.3.0
      - name: install dependencies
        run: flutter packages get
      - name: run tests
        run: flutter test_driver